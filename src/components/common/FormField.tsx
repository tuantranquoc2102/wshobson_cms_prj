'use client';

// Convenience re-export so feature code can `import { FormField, FormItem... }`
// from a single module.
export {
  Form,
  FormField,
  FormItem,
  FormLabel,
  FormControl,
  FormDescription,
  FormMessage,
} from '@/components/ui/form';
